# Root — używane przez docker compose (interpolacja ${OPENAI_API_KEY})
# Skopiuj do .env i uzupełnij, jeśli używasz DeepEval / pilota OpenAI w API.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_CHAT_MODEL=gpt-4o-mini
